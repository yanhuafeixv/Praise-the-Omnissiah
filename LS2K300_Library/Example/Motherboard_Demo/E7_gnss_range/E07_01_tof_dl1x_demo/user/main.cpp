
/*********************************************************************************************************************
* LS2K0300 Opensourec Library 即（LS2K0300 开源库）是一个基于官方 SDK 接口的第三方开源库
* Copyright (c) 2022 SEEKFREE 逐飞科技
*
* 本文件是LS2K0300 开源库的一部分
*
* LS2K0300 开源库 是免费软件
* 您可以根据自由软件基金会发布的 GPL（GNU General Public License，即 GNU通用公共许可证）的条款
* 即 GPL 的第3版（即 GPL3.0）或（您选择的）任何后来的版本，重新发布和/或修改它
*
* 本开源库的发布是希望它能发挥作用，但并未对其作任何的保证
* 甚至没有隐含的适销性或适合特定用途的保证
* 更多细节请参见 GPL
*
* 您应该在收到本开源库的同时收到一份 GPL 的副本
* 如果没有，请参阅<https://www.gnu.org/licenses/>
*
* 额外注明：
* 本开源库使用 GPL3.0 开源许可证协议 以上许可申明为译文版本
* 许可申明英文版在 libraries/doc 文件夹下的 GPL3_permission_statement.txt 文件中
* 许可证副本在 libraries 文件夹下 即该文件夹下的 LICENSE 文件
* 欢迎各位使用并传播本程序 但修改内容时必须保留逐飞科技的版权声明（即本声明）
*
* 文件名称          main
* 公司名称          成都逐飞科技有限公司
* 适用平台          LS2K0300
* 店铺链接          https://seekfree.taobao.com/
*
* 修改记录
* 日期              作者           备注
* 2025-12-27        大W            first version
********************************************************************************************************************/

#include "zf_common_headfile.hpp"

// *************************** 例程硬件连接说明 ***************************
//  将2K300核心板插到主板上面，确保插到底核心板与主板插座间没有缝隙即可
//  将2K300核心板插到主板上面，确保插到底核心板与主板插座间没有缝隙即可
//  将2K300核心板插到主板上面，确保插到底核心板与主板插座间没有缝隙即可
//  使用本历程，就需要使用我们逐飞科技提供的内核。
//  使用本历程，就需要使用我们逐飞科技提供的内核。
//  使用本历程，就需要使用我们逐飞科技提供的内核。
// 
// 接入 DL1A或者DL1B
// 接入 DL1A或者DL1B
// 接入 DL1A或者DL1B
// 改例程序自动判断是哪一个TOF传感器
//      模块管脚             单片机管脚
//      SCL/SPC             查看 seekfree_2k0300_coreboard.dts 文件中 i2c4 节点定义 默认 GPIO49
//      SDA/DSI             查看 seekfree_2k0300_coreboard.dts 文件中 i2c4 节点定义 默认 GPIO48
//      XS                  查看 seekfree_2k0300_coreboard.dts 文件中 i2c4 节点定义 默认 GPIO38
//      GND                 电源地 GND
//      3V3                 电源 3V3
// *************************** 例程测试说明 ***************************
// 1.先将姿态传感器插入主板，运行代码。
// 
// 2.打开终端，使用115200波特率
//
// 4.终端中看到
//      =====================================
//      DL1X 100ms 定时采集 Demo
//      =====================================
//      DL1X 初始化成功！识别到：ZF_DEVICE_DL1A
//      pit init success, period = 100 ms
//      PIT定时器初始化成功，100ms周期采集DL1X数据...
//      =====================================
//      DL1X 原始距离数据：123
//      DL1X 原始距离数据：125
//      DL1X 原始距离数据：124
//      ...
//      ...
// 5.移动旋转 姿态传感器 就会看到数值变化
// 
// 如果发现现象与说明严重不符 请参照本文件最下方 例程常见问题说明 进行排查
// **************************** 代码区域 ****************************


//-------------------------------------------------------------------------------------------------------------------
// 全局变量定义
//-------------------------------------------------------------------------------------------------------------------
zf_device_dl1x dl1x_dev;                  // DL1X设备对象
enum dl1x_device_type_enum dl1x_dev_type;  // DL1X设备类型
zf_driver_pit dl1x_pit_timer;             // PIT定时器对象（用于100ms定时采集）
volatile int16 dl1x_distance_raw = 0;      // 定时采集的距离原始数据（volatile防止编译器优化）

//-------------------------------------------------------------------------------------------------------------------
// 函数简介  PIT定时器100ms周期回调函数
// 参数说明  无
// 返回参数  无
// 使用示例  注册到PIT定时器，定时自动执行
// 备注信息  回调函数在定时器线程中执行，需精简高效，仅做数据读取（不做耗时打印操作）
//-------------------------------------------------------------------------------------------------------------------
void dl1x_pit_callback(void)
{
    // 仅读取DL1X距离数据，存入全局变量（主循环负责打印，避免回调内耗时）
    if(NO_FIND_DEVICE != dl1x_dev_type)  // 仅当设备初始化成功时读取
    {
        dl1x_distance_raw = dl1x_dev.get_distance();
    }
}

//-------------------------------------------------------------------------------------------------------------------
// 函数简介  程序入口主函数
//-------------------------------------------------------------------------------------------------------------------
int main(int, char**) 
{
    // 初始化系统基础功能，打印欢迎信息
    printf("=====================================\r\n");
    printf("DL1X 100ms 定时采集 Demo\r\n");
    printf("=====================================\r\n");

    // DL1X传感器初始化
    dl1x_dev_type = dl1x_dev.init();

    // 判断DL1X初始化结果
    if(NO_FIND_DEVICE == dl1x_dev_type)
    {
        printf("错误：未找到DL1X设备，请检查硬件连接！\r\n");
        return -1;
    }
    else
    {
        printf("DL1X 初始化成功！识别到：");
        if(ZF_DEVICE_DL1A == dl1x_dev_type) printf("ZF_DEVICE_DL1A\r\n");
        else if(ZF_DEVICE_DL1B == dl1x_dev_type) printf("ZF_DEVICE_DL1B\r\n");
    }

    // 初始化PIT定时器，100ms周期，注册回调函数
    int pit_init_ret = dl1x_pit_timer.init_ms(100, dl1x_pit_callback);
    if(pit_init_ret != 0)
    {
        printf("错误：PIT定时器初始化失败！\r\n");
        return -1;
    }
    printf("PIT定时器初始化成功，100ms周期采集DL1X数据...\r\n");
    printf("=====================================\r\n");

    // 主循环，周期性打印定时采集到的距离数据
    while(1)
    {
        // 打印全局变量中存储的定时采集数据
        printf("DL1X 原始距离数据：%d\r\n", dl1x_distance_raw);

        // 延时1000ms（1秒）打印一次，避免串口刷屏（与采集周期解耦）
        system_delay_ms(1000);
    }

    // 正常退出（主循环无限执行，此处仅为语法兼容）
    return 0;
}


// **************************** 代码区域 ****************************

// *************************** 例程常见问题说明 ***************************
// 遇到问题时请按照以下问题检查列表检查
// 
// 问题1：终端提示未找到xxx文件
//      使用本历程，就需要使用我们逐飞科技提供的内核，否则提示xxx文件找不到
//      使用本历程，就需要使用我们逐飞科技提供的内核，否则提示xxx文件找不到
//      使用本历程，就需要使用我们逐飞科技提供的内核，否则提示xxx文件找不到